const student = { name: "anik", age: 23 };

const name = 'Hero';
const age = 34;
// const person = {name, age}; console.log(person);

// What is the value of ratName?

let person =null;
console.log(person ? person : "person is null");


const n = [1,23,4]

const a = n.filter(x=>x)

console.log(a);

const obj = {a:1,b:2};
console.log(Object.keys(obj).length===0);